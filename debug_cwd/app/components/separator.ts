import * as React from 'mini-react';

/**
 * Symbolic element that represents a separator.
 * This element is used to create a separator between elements for certain UI components such as dropdown menus.
 *
 * @returns A null element.
 */
export const Separator: React.FC = () => {
    return null;
};
